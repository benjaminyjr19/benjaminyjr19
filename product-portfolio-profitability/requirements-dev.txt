# Development dependencies. These are needed to run the test suite and are not
# required to run the analysis itself.
# Install with: pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest==8.3.3
